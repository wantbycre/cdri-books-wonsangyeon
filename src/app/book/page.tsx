import BooksList from "./BooksList";
import BooksSearch from "./BooksSearch";

const Book = () => {
    return (
        <>
            <BooksSearch />
            <BooksList />
        </>
    );
};

export default Book;
